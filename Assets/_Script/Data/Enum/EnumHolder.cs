
public enum Food
{
    Spicy = 0,
    Cold = 1
}

public enum Trait
{
    Kind = 0,
    Generous = 1,
    Talkative = 2
}

public enum Target
{
    Dish = 0,
    Person = 1,
    Cell = 2
}

public enum Scope
{
    Self = 0,
    Adjacent = 1
}

public enum Comparator
{
    AtLeast = 0,
    AtMost = 1,
    Exact = 2
}

public enum LogicalOperator
{
    And = 0, 
    Or = 1
}